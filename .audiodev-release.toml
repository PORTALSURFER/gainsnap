# AudioDev release contract v1.
schema_version = 1
slug = "gainsnap"
display_name = "GainSnap"
category = "Utility"
tagline = "Toggle peak matching for Ableton tracks"
description = "GainSnap measures an incoming track peak while Match is enabled, applies the gain needed to reach a chosen target when Match is disabled, and holds that gain until the next measurement."
repository = "PORTALSURFER/gainsnap"
toybox_rev = "1c40aeee6062b3bcebc2799fa2ab65cf36d160a2"
manifest_schema = 2
formats = ["clap", "vst3"]
# Public PortalSurfer releases remain macOS arm64; the release workflow also uploads unsigned Windows Actions artifacts.
platform = "macos"
architectures = ["arm64"]
